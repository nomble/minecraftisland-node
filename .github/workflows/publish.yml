name: Create NPM Package

on:
  workflow_dispatch:
  release:
    types: [created]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          registry-url: https://npm.pkg.github.com/nomble
      - name: Install NPM dependencies
        run: npm install
      - name: Run tests
        run: npm run test
      - name: Create NPM package
        run: npm run build
      - name: Publish NPM package
        run: npm publish --registry https://npm.pkg.github.com/nomble --verbose
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
